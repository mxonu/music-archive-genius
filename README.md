# 🎵 Музыкальный архив Genius

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![Flask](https://img.shields.io/badge/Flask-2.3+-green.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)
![Stars](https://img.shields.io/github/stars/mxonu/music-archive-genius?style=social)

Веб-приложение для автоматического сбора музыкальных артистов, песен и текстов с Genius API. Удобный поиск, красивый интерфейс и REST API для доступа к данным.

## 📖 Описание

**Music Archive Genius** — это полнофункциональное Flask-приложение, которое автоматически собирает данные о музыкантах и их песнях с Genius.com, сохраняет полные тексты песен в JSON-формате, предоставляет красивый веб-интерфейс с поиском и REST API для программного доступа к базе данных. Проект создан в рамках изучения Python, веб-разработки и работы с API.

## ✨ Возможности

**Сбор данных:** автоматический сбор топ-100 популярных артистов, интерактивный режим для добавления артистов вручную, получение полных текстов песен, сохранение метаданных (год, ссылки, изображения) и статистика собранного архива.

**Веб-интерфейс:** адаптивный дизайн с градиентным фоном, полнотекстовый поиск по артистам, названиям и текстам песен, просмотр всех артистов и песен, раскрывающиеся тексты песен и статистика (количество артистов и песен).

**REST API:** получение списка всех артистов через GET /api/artists, информация об артисте через GET /api/artist/<name>, поиск по базе через GET /api/search?q=query и статистика архива через GET /api/stats.

## 🛠️ Технологии

Python 3.8+ (основной язык программирования), Flask 3.0 (веб-фреймворк), lyricsgenius 3.7.5 (клиент для Genius API), BeautifulSoup4 (парсинг HTML), Jinja2 (шаблоны HTML) и python-dotenv (управление переменными окружения).

## 📦 Установка

**Требования:** Python 3.8 или выше, pip (менеджер пакетов Python) и Genius API токен (получить можно на https://genius.com/api-clients).

**Шаг 1 — Клонируй репозиторий:** выполни команды `git clone https://github.com/mxonu/music-archive-genius.git` и `cd music-archive-genius`.

**Шаг 2 — Создай виртуальное окружение:** выполни `python3 -m venv venv` (на Mac/Linux) или `python -m venv venv` (на Windows), затем активируй его командой `source venv/bin/activate` (на Mac/Linux) или `venv\Scripts\activate` (на Windows). В начале строки терминала должно появиться (venv).

**Шаг 3 — Установи зависимости:** выполни `pip install -r requirements.txt` и дождись установки всех библиотек (30-60 секунд).

**Шаг 4 — Настрой .env файл:** создай файл .env в корне проекта командой `touch .env` (Mac/Linux) или `type nul > .env` (Windows), затем открой его в текстовом редакторе и добавь строку `GENIUS_TOKEN=твой_токен_здесь` без кавычек и пробелов. Чтобы получить токен, перейди на https://genius.com/api-clients, войди или зарегистрируйся, нажми "New API Client", заполни форму (App Name: My Music Archive, App Website URL: http://localhost:5000, Redirect URI: http://localhost:5000/callback), нажми Save и скопируй Client Access Token.

## 🚀 Использование

**Сбор музыки с Genius:** запусти `python scraper.py` и выбери один из трёх режимов. Режим 1 (Интерактивный) позволяет вводить артистов вручную — просто введи имя артиста (например Queen), затем введи ещё имена или напиши exit для завершения, после чего укажи количество песен на артиста (например 10). Режим 2 (Предзагруженные артисты) автоматически собирает 100+ популярных артистов из разных жанров — просто укажи количество песен на артиста (например 5) и программа начнёт сбор (это займёт 2-5 минут). Режим 3 (Статистика) показывает уже собранные данные без запуска нового сбора.

**Запуск веб-приложения:** выполни команду `python app.py` и увидишь сообщение о запуске сервера на http://127.0.0.1:5000, затем открой браузер и перейди по адресу http://127.0.0.1:5000 или просто localhost:5000. Для остановки сервера нажми Ctrl+C в терминале.

## 🔗 API Endpoints

**Получить всех артистов:** отправь GET запрос на /api/artists и получишь JSON массив с объектами вида {"name": "Imagine Dragons", "country": "Unknown", "songs_count": 5, "url": "/artist/Imagine%20Dragons"}.

**Получить информацию об артисте:** отправь GET запрос на /api/artist/<artist_name>, например curl http://127.0.0.1:5000/api/artist/Queen.

**Поиск:** отправь GET запрос на /api/search?q=<query>, например curl "http://127.0.0.1:5000/api/search?q=Queen" для поиска по артисту, curl "http://127.0.0.1:5000/api/search?q=Bohemian" для поиска по песне или curl "http://127.0.0.1:5000/api/search?q=love" для поиска по тексту.

**Статистика:** отправь GET запрос на /api/stats и получишь JSON объект вида {"total_artists": 100, "total_songs": 500, "artists_by_country": {"Unknown": 100}, "last_updated": "2025-12-02T23:00:00"}.

## 📁 Структура проекта

Проект состоит из следующих файлов и папок: .env (секретные переменные, НЕ ЗАГРУЖАТЬ В GIT), .env.example (пример .env файла), .gitignore (игнорируемые файлы), LICENSE (лицензия MIT), README.md (документация проекта), requirements.txt (зависимости Python), music_data.json (база данных, создаётся автоматически), scraper.py (программа для сбора музыки с Genius API), app.py (Flask веб-приложение), папка templates содержащая index.html (главная страница), artist.html (страница артиста) и 404.html (страница ошибки), папка static содержащая style.css (стили CSS).

## ⚙️ Конфигурация

**Настройка количества артистов:** открой scraper.py в текстовом редакторе и найди функцию main(), затем измени список popular_artists, добавив своих артистов в формате ["Твой Артист 1", "Твой Артист 2", ...].

**Изменение порта Flask:** открой app.py и в конце файла найди строку app.run(debug=True, port=5000), затем измени 5000 на любой другой порт, например 8080.

## ⚠️ Частые ошибки

**Ошибка ModuleNotFoundError: No module named 'flask'** означает что виртуальное окружение не активировано или зависимости не установлены — выполни `source venv/bin/activate` и затем `pip install -r requirements.txt`.

**Ошибка GENIUS_TOKEN not found** означает что файл .env не создан или не содержит токен — проверь файл командой `cat .env` (Mac/Linux) или `type .env` (Windows), в нём должна быть строка GENIUS_TOKEN=твой_токен без кавычек.

**Ошибка Address already in use** означает что порт 5000 уже занят другим приложением — останови процесс командой `lsof -ti:5000 | xargs kill -9` (Mac/Linux) или `netstat -ano | findstr :5000` затем `taskkill /PID <номер> /F` (Windows).

**Ошибка JSONDecodeError** означает что файл music_data.json повреждён — удали его командой `rm music_data.json` и запусти `python scraper.py` заново для создания нового файла.

## 🚀 Запуск на другом компьютере

**Вариант 1 — Через GitHub:** на новом компьютере установи Python 3.8+ (с python.org или через brew install python на Mac), установи Git (с git-scm.com или через brew install git на Mac), затем выполни `git clone https://github.com/mxonu/music-archive-genius.git` и `cd music-archive-genius`, создай виртуальное окружение командой `python3 -m venv venv` (или `python -m venv venv` на Windows), активируй его командой `source venv/bin/activate` (Mac/Linux) или `venv\Scripts\activate` (Windows), установи зависимости командой `pip install -r requirements.txt`, создай файл .env и добавь в него свой Genius токен (получи на genius.com/api-clients), запусти `python scraper.py` для сбора музыки (выбери режим 2 и укажи количество песен), затем запусти `python app.py` и открой в браузере localhost:5000.

**Вариант 2 — Через ZIP архив:** скачай архив проекта, распакуй его в любое место, открой терминал в папке проекта и выполни те же шаги начиная с создания виртуального окружения (python3 -m venv venv, активация, установка зависимостей, создание .env, запуск scraper.py и app.py).

**Частые проблемы на новом компьютере:** если появляется ошибка python: command not found попробуй python3 --version или установи Python с официального сайта, если не активируется venv на Windows с ошибкой "running scripts is disabled" открой PowerShell от администратора и выполни Set-ExecutionPolicy RemoteSigned, если появляется ModuleNotFoundError убедись что виртуальное окружение активировано (должно быть (venv) в начале строки терминала) и выполни pip install -r requirements.txt, если GENIUS_TOKEN not found проверь что файл .env существует и содержит правильный токен без кавычек.
